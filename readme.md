# FTP Brute Force

## Usage
```
python ftp.py [-h] [-t HOST] [-p PORT] [-u USERNAME] [-d DICTIONARY]

arguments:
  -t HOST, --host HOST
  -u USERNAME, --username USERNAME
  -d DICTIONARY, --dictionary DICTIONARY

optional arguments:
  -h, --help
  -p PORT, --port PORT
```